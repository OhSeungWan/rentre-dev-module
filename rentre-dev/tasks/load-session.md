# Load Session Task - 저장된 세션 복원

Serena Memory에 저장된 워크플로우 세션을 불러와 작업을 재개하는 태스크

## 용도

이 태스크는 이전 세션에서 저장한 워크플로우 상태를 복원하여 **중단된 작업을 이어서 진행**할 때 사용됩니다.

**사용 시나리오:**

- 새 세션 시작 시 이전 작업 재개
- 컨텍스트 초기화 후 워크플로우 복원
- 에이전트의 `*resume` 또는 `*load` 명령으로 실행

## 입력 파라미터

```yaml
# 옵션 1: 특정 세션 지정
memory_name: "dev-backlog-session-20231215-143022"

# 옵션 2: 최신 세션 자동 선택
auto_latest: true
workflow_filter: "dev-backlog"  # 선택적 - 특정 워크플로우만 필터
```

## 실행 단계

### 1. 저장된 세션 목록 확인

#### 1.1 세션 목록 조회

```
mcp__serena__list_memories 호출
```

#### 1.2 세션 필터링

`*-session-*` 패턴으로 세션 파일만 필터:

```yaml
available_sessions:
  - name: "dev-backlog-session-20231215-143022"
    workflow: "dev-backlog"
    saved_at: "2023-12-15T14:30:22Z"
  - name: "prepare-backlog-session-20231214-091500"
    workflow: "prepare-backlog"
    saved_at: "2023-12-14T09:15:00Z"
```

### 2. 세션 선택

#### 2.1 memory_name이 지정된 경우

해당 세션 직접 로드

#### 2.2 auto_latest가 true인 경우

```markdown
## 📋 저장된 세션 목록

| # | 워크플로우 | 저장 시각 | 진행률 |
|---|-----------|----------|--------|
| 1 | dev-backlog | 2023-12-15 14:30 | 3/7 (43%) |
| 2 | prepare-backlog | 2023-12-14 09:15 | 5/5 (100%) |

---

**[1]** 가장 최근 세션 복원 (dev-backlog)
**[번호]** 특정 세션 선택
**[C]** 취소
```

### 3. 세션 데이터 로드

```
mcp__serena__read_memory 호출:
- memory_file_name: "{selected_memory_name}"
```

### 4. 세션 상태 복원

#### 4.1 복원된 데이터 파싱

```yaml
restored_session:
  workflow_name: "dev-backlog"
  workflow_path: "{project-root}/.bmad/rentre-dev/workflows/dev-backlog"
  current_step: 3
  total_steps: 7
  session_data:
    backlog_id: "RENT-123"
    backlog_title: "사용자 인증 기능 구현"
    current_subtask: 2
    completed_subtasks: [1]
    # ... 기타 데이터
  custom_context:
    # 워크플로우별 추가 컨텍스트
```

#### 4.2 복원 요약 출력

```markdown
## ✅ 세션 복원 완료

### 📊 복원된 상태

| 항목 | 값 |
|------|-----|
| **워크플로우** | {workflow_name} |
| **저장 시각** | {saved_at} |
| **진행률** | {current_step}/{total_steps} ({progress}%) |
| **백로그** | {backlog_title} |

### 📝 이전 세션 요약

**완료된 스텝:**
{completed_steps_summary}

**마지막 작업 내용:**
{last_activity_summary}

**저장된 노트:**
{notes_if_any}

---

### ▶️ 다음 단계

**Step {next_step}: {step_title}**

{step_description_preview}

---

**[Y]** Step {next_step}부터 진행
**[R]** 이전 스텝 다시 보기
**[C]** 취소하고 새로 시작
```

### 5. 워크플로우 재개

사용자 확인 후:

1. 복원된 `session_data`를 워크플로우 컨텍스트에 주입
2. `current_step`에 해당하는 스텝 파일 로드
3. 워크플로우 실행 재개

## 반환값

```yaml
success: true | false
restored_session:
  workflow_name: "dev-backlog"
  current_step: 3
  total_steps: 7
  session_data: { ... }
  custom_context: { ... }
resume_from: "step-03-implementation.md"
error: null | "에러 메시지"
```

## 에러 처리

### 세션을 찾을 수 없는 경우

```markdown
## ⚠️ 세션을 찾을 수 없습니다

**요청한 세션:** `{memory_name}`

### 가능한 원인
- 세션 이름이 잘못되었습니다
- 세션이 삭제되었습니다
- 다른 프로젝트의 세션입니다

### 사용 가능한 세션
{available_sessions_list}

---

**[번호]** 다른 세션 선택
**[N]** 새 워크플로우 시작
```

### 세션 데이터 손상된 경우

```markdown
## ❌ 세션 복원 실패

**원인:** 세션 데이터가 손상되었거나 형식이 맞지 않습니다.

### 복구 옵션

**[1]** 부분 복원 시도 (가능한 데이터만)
**[2]** 세션 삭제 후 새로 시작
**[3]** 수동으로 상태 입력
```

## 에이전트 메뉴 통합

```xml
<item cmd="*resume">[R] Resume - 최근 세션 복원</item>
<item cmd="*load">[L] Load Session - 세션 선택하여 복원</item>
<item cmd="*sessions">[SS] Sessions - 저장된 세션 목록</item>
```

## 연계

- **save-session (Task)**: 세션 저장
- **모든 워크플로우**: 복원 후 해당 워크플로우 스텝으로 이동
- **에이전트 메뉴**: `*resume`, `*load` 명령으로 접근
