# Backlog Guide Summary

> Generated by Rentre-Dev Navigator on {{date}}
> Database: {{database_name}}

## Overview

| 항목              | 값                   |
| ----------------- | -------------------- |
| 데이터베이스 이름 | {{database_name}}    |
| 데이터베이스 ID   | {{database_id}}      |
| 노션 URL          | {{database_url}}     |
| 백로그 유형 수    | {{types_count}}      |
| 전체 속성 수      | {{properties_count}} |
| 관계 속성 수      | {{relations_count}}  |

## Backlog Types at a Glance

| 유형 | 아이콘 | 설명 | 상위 유형 | 하위 유형 |
| ---- | ------ | ---- | --------- | --------- |

{{types_table}}

## Hierarchy Overview

```
{{hierarchy_diagram}}
```

## Core Properties

모든 백로그 유형에서 공통으로 사용되는 속성:

| 속성명 | 타입 | 설명 | 필수 |
| ------ | ---- | ---- | ---- |

{{core_properties_table}}

## Relation Properties

백로그 간 연결 관계를 정의하는 속성:

| 속성명 | 연결 대상 | 방향 | 용도 |
| ------ | --------- | ---- | ---- |

{{relation_properties_table}}

## Quick Navigation for Agents

### 유형별 백로그 찾기

```notion-api
POST /databases/{database_id}/query
Filter: { "property": "Type", "select": { "equals": "{type_name}" } }
```

### 상위 백로그 찾기

```notion-api
1. GET current item's relation property
2. GET /pages/{parent_id}
```

### 하위 백로그 찾기

```notion-api
POST /databases/{database_id}/query
Filter: { "property": "{child_relation}", "relation": { "contains": "{current_id}" } }
```

## Individual Guides

각 유형별 상세 가이드:

{{individual_guides_list}}

## See Also

- [hierarchy-map.md](./hierarchy-map.md) - 전체 계층 구조 상세
- [Notion API Reference](https://developers.notion.com/reference)

---

_이 가이드는 Navigator 에이전트가 자동 생성했습니다._
_수정이 필요하면 `*edit-guide` 명령을 사용하세요._
