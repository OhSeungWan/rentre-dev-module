# Backlog Info Template v2.0 - Block-based Traceability
# Generated by: prepare-backlog workflow
# Schema Version: 2.0

schema_version: "2.0"

# === 기본 메타정보 ===
backlog_id: "{backlog_id}"
title: "{title}"
type: "{type}" # Epic | Story | Task | Bug | Subtask
status: "prepared"
notion_id: "{notion_id}"
created_at: "{created_at}"
prepared_at: "{prepared_at}"

# === 📦 원본 컨텐츠 블록 ===
# 원본 내용을 논리적 단위로 분할한 블록들
# 분해 시 이 블록들이 하위 백로그로 매핑됨
content_blocks: []
  # 예시:
  # - id: "BLK-001"
  #   type: "description"
  #   lines: [1, 5]
  #   source: "notion_description"
  #   content: |
  #     원본 내용...
  #   tags: []

# === 📋 구조화된 요구사항 ===
# 블록에서 추출된 요구사항 (블록 참조 포함)
requirements: []
  # 예시:
  # - id: "REQ-001"
  #   summary: "요구사항 요약"
  #   type: "functional"
  #   priority: "high"
  #   source_blocks: ["BLK-001", "BLK-002"]
  #   constraints:
  #     - "제약 조건 1"
  #     - "제약 조건 2"

# === ✅ 수용 기준 ===
# 블록에서 추출된 수용 기준 (블록 참조 포함)
acceptance_criteria: []
  # 예시:
  # - id: "AC-001"
  #   summary: "수용 기준 요약"
  #   source_blocks: ["BLK-003"]
  #   testable: true
  #   related_requirements: ["REQ-001"]

# === 🔗 계층 정보 ===
hierarchy:
  parent:
    id: "{parent_id}"
    title: "{parent_title}"
    type: "{parent_type}"
    notion_id: "{parent_notion_id}"
  children: [] # 분해 후 채워짐
  connections:
    blocking: []
    blocked_by: []
    related: []

# === 📊 커버리지 메트릭 ===
# 분해 후 자동 계산됨
coverage:
  total_blocks: 0
  covered_blocks: 0
  coverage_percent: 0
  uncovered_blocks: []
  shared_blocks: []
  validation_passed: false

# === 🎨 컨텍스트 ===
context:
  figma:
    url: "{figma_url}"
    file_key: "{figma_file_key}"
    node_id: "{figma_node_id}"
  references: []
  unclear_items: []

# === 📝 원본 데이터 (하위 호환) ===
raw:
  description: |
    {raw_description}
  acceptance_criteria: |
    {raw_acceptance_criteria}
